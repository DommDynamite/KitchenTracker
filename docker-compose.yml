version: '3.8'

services:
  kitchen-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kitchen-tracker
    restart: unless-stopped
    ports:
      - "3002:3001"
    volumes:
      - ./backend/data:/app/backend/data
    environment:
      - PORT=3001
      - NODE_ENV=production
